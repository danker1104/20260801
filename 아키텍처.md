# GD-Food 아키텍처

## 1. 아키텍처 선택
- 선택안: 단일 웹 아키텍처(모놀리식)
- 핵심 UX 요구: 앱 진입 즉시 지도 노출

## 2. 기술 스택
- 프론트엔드: HTML, CSS, JavaScript
- 백엔드: Python FastAPI
- 데이터베이스: PostgreSQL (개발/테스트 SQLite 가능)
- 외부 연동: SK Open API

## 3. 전제 조건
1. 로그인 완료 상태 가정
2. 인증 UI/인증 로직 제외
3. userId는 세션 또는 요청 헤더 전달

## 4. 핵심 기능
1. 앱 첫 진입 즉시 지도 표시
2. 위치 기반 1km 추천
3. 카테고리 필터 추천
4. 자체 리뷰 작성/조회 및 추천 반영

## 5. 서비스 구성
1. 클라이언트
- 앱 로드 직후 지도 컴포넌트 우선 렌더링
- 권한 허용 전 기본 중심 좌표 지도 표시
- 권한 허용 후 주변 식당 조회 트리거
- 권한 미허용 시 주변 조회 API 미호출

2. API 서버
- SK Open API 호출
- 1km 필터/카테고리 필터/정렬
- 추천 점수 계산
- 리뷰 CRUD 및 통계 갱신

3. 데이터베이스
- Restaurant, Review, ReviewStats 저장

## 6. 요청 흐름
### 6.1 초기 진입
1. 클라이언트가 지도 먼저 렌더링
2. 위치 권한 요청
3. 허용 시 nearby API 호출
4. 미허용 시 기본 중심 지도 + 안내 메시지 노출
5. 미허용 상태에서는 리스트 데이터 로드를 강제하지 않는다.

### 6.2 주변 식당 조회
1. GET /api/restaurants/nearby 호출
2. SK Open API 조회 및 정규화
3. 리뷰 통계 결합 + 추천 점수 계산
4. 지도 핀/리스트 동시 반환

### 6.3 리뷰 작성
1. POST /api/restaurants/{restaurantId}/reviews
2. 서버가 세션/신뢰 헤더에서 userId 추출
3. 입력 검증 후 저장
4. 트랜잭션 내 통계 갱신 및 최신 점수 반영

## 7. 핵심 API
1. GET /api/restaurants/nearby
- query: lat, lng, radius=1000, category(optional), sort(optional)

2. GET /api/restaurants/{restaurantId}/reviews
- query: page(optional), size(optional)

3. POST /api/restaurants/{restaurantId}/reviews
- body: rating(1~5), content
- userId는 서버가 세션/신뢰 헤더에서 주입

## 8. 추천 점수 규칙
- recommendScore = 0.4 x distanceScore + 0.3 x externalScore + 0.3 x reviewScore

## 9. 비기능 요구사항
1. 성능: 지도 첫 렌더링 체감 지연 최소화
2. 안정성: SK API 실패 시 재시도 및 fallback
3. 보안: API 키 서버 환경변수 관리

## 10. 데이터 무결성 제약
1. Restaurant.externalId 유니크
2. Review.rating CHECK (1~5)
3. Review의 restaurantId+userId 단일 리뷰 정책
4. Review 저장과 ReviewStats 갱신 트랜잭션 보장

## 11. MVP 우선순위
1. 첫 진입 지도 노출
2. 1km 추천 API
3. 카테고리/정렬
4. 리뷰 작성/조회
5. 에러/로딩/접근성 고도화